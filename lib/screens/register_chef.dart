import 'package:flutter/material.dart';

class RegisterChef extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('تسجيل الشيف'),),
    );
  }
}
