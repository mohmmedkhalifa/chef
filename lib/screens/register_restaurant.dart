import 'package:flutter/material.dart';

class RegisterRestaurant extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('تسجيل المطعم'),),
    );
  }
}
